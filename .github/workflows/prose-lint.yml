name: PR Lint
on:
  pull_request:
    branches: 
      - main
    paths:
      - '**/*.md'
jobs:
  pr_lint:
    runs-on: ubuntu-latest
    steps:
    - uses: vijaykramesh/pr-lint-action@v2.3
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

